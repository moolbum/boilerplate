import React from 'react';

interface ToastProps {}

function Toast(props: ToastProps) {
  /**
   * type: default, success, error, warring
   * pauseOnHover: Hover시 시간이 줄어들지 안줄어들지
   * position: topCenter, topLeft, topRight, bottomCenter, bottomLeft, bottomRight
   * limit: toast 최대 개수
   * autoClose: 자동으로 toast꺼지는 시간
   * isProgressBar: toast 하단 progressBar
   * stack: 스택형으로 toast쌓을지
   * closeOnClick: close 버튼 클릭으로 닫기
   */

  return <div />;
}

export default Toast;
